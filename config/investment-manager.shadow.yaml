extends: investment-manager.yaml

# 仅覆盖 Shadow 运行差异；研究基线仍可回放，未通过盲测的策略不进入前瞻运行。
strategy:
  enabled: false

temporal:
  # Shadow 与 Mock/测试隔离，避免相同稳定 Workflow ID 绑定到另一套事实库。
  namespace: shadow-20260818

deployment:
  stage: SHADOW
  shadow_market_data_enabled: true
