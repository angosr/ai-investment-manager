# Codex 市场信息研判模板

先调用 `get_system_status` 确认数据日期与源状态，再调用 `aggregate_news` 对今天的数据跨源去重。RSS 必须分层、分开查询，避免某个高频聚合源占满结果：

- 一手政策与数据：`fed-press`、`sec-press`、`ecb-press`、`bea-data`；
- 市场与研究：`yahoo-finance`、`coindesk`、`arxiv-qfin`；
- 发现层：`google-cn-macro`、`google-us-macro`、`google-ai-chips`。

按需要用 `search_news` 和 `read_articles_batch` 补充原文。Google News 只算线索，不算独立信源；同一通讯社被多个站点转载也只能算一个来源。一手文件优先于媒体解读，并检查原始发布时间，不能把今天入库的旧文章误当成今天发生。

请生成“信息驱动的观察信号”，不要直接给下单指令。每个候选主题必须包含：

1. 发生了什么，以及最早来源和至少一个真正独立的交叉来源；
2. 影响链：事件 → 宏观/行业变量 → 可能受影响资产；
3. 方向、时间窗口、置信度（0–100）与支持证据；
4. 反证、数据盲区、失效条件；
5. 需要用真实行情验证的条件（价格、成交量、波动率、期限结构等）；
6. 标注“事实 / 来源观点 / 模型推断”，禁止把新闻情绪当成可直接交易的 alpha。

若来源少于两个、发布时间不明、只有转载、或价格已明显先行，则结论只能是“观察”，不得升级为信号。
